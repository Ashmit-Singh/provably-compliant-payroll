import React, { memo } from 'react';
import AuditorPortal from '../blockchain/AuditorPortal';

const AuditorPortalCard = memo(() => (
  <div className="mt-8">
    <AuditorPortal />
  </div>
));

export default AuditorPortalCard;
